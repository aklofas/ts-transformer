# ThreadSanitizer suppressions for the nightly CI run.
#
# Format: one suppression per line. race:<func-name-substring> /
# mutex:<...> / thread:<...>. See:
# https://github.com/google/sanitizers/wiki/ThreadSanitizerSuppressions
#
# Adding a suppression: prepend a comment with the date, the symptom,
# the linked CI run URL, and the planned-fix follow-up. Remove the
# suppression in the same commit that ships the fix.
#
# This file starts empty. Entries land reactively as the first nightly
# runs surface real findings vs false positives.

# 2026-07-24 — tokio io-driver internals (false-positive class, not our
# code). Adding tst-rtp/tst-hls to the TSan scope put a tokio
# multi-thread io driver under TSan for the first time; 50 warnings
# across 4 signatures, ALL with racing accesses inside
# tokio::runtime::io::scheduled_io (readiness CAS/load, waker-slot
# replace via ScheduledIo::wake, waiter-list drain_filter). Zero tst-*
# frames at any racing access (ours appear only as task-spawn ancestry);
# no assertion failures (the 9 test "failures" were TSan's exit code);
# same tests ASan-clean. ScheduledIo's hand-rolled readiness/lock
# protocol is loom-verified upstream but uses ordering idioms TSan
# cannot model (tokio-rs/tokio#2273 documents the fence-vs-TSan class).
# Evidence: caught in local pre-push verification (never red in CI);
# first CI run exercising this entry = the PR #126 branch dispatch,
# https://github.com/aklofas/ts-transformer/actions/runs/30140030852
# The pattern is deliberately module-scoped, NOT
# race:tokio* — task-harness frames appear in genuine app-race stacks
# and must stay unsuppressed.
# Remove/re-test: on any tokio upgrade that restructures the io driver
# (delete the entry, dispatch the tsan job, re-add only if it re-fires).
race:tokio::runtime::io::scheduled_io

# 2026-08-19 — librist-internal races (tsan-native harvest, WP-SAN-5).
# First TSan run over the sanitized native builds surfaced 5 reports in
# 4 tst-rist live-loopback test processes; every RACING frame on both
# sides sits inside librist C code — tst-rist Rust frames appear only as
# the public-API caller (send_bytes/close). Three classes, suppressed by
# the librist-internal function names at the racing accesses (NOT a
# blanket race:rist* — rist_sys FFI frames ride genuine app-race stacks
# and must stay unsuppressed):
#   1. receiver flow handoff — rist_receiver_recv_data (rist-common.c:595)
#      read vs rist_receiver_associate_flow/receiver_pthread_dataout
#      (flow.c:202) write, each under a DIFFERENT librist mutex (M0/M1).
#   2. evsocket teardown — ctx_del/evsocket_destroy (libevsocket.c:92)
#      during rist_destroy racing the evsocket loop thread's mutex use.
#   3. sender stats — rist_sender_enqueue (udp.c:930) app-thread write vs
#      rist_sender_flow_statistics (stats.c:54) stats-thread read.
# All three are upstream synchronization contracts internal to librist
# 0.2.18 (upstream-report candidates; distinct from the 0.2.18-fixed
# teardown-hang class). Evidence: harvested in a local pre-CI run
# (2026-08-19, 514/518 with the only failures being TSan exit codes from
# these reports); first CI run exercising this block = the WP-SAN-5
# branch dispatch, all 4 sanitizer jobs green:
# https://github.com/aklofas/ts-transformer/actions/runs/32334048232
# Remove/re-test: on any librist bump — delete this block, dispatch
# tsan-native, re-add only what re-fires.
# 2026-09-03 re-test on librist 0.2.20 (the 0.2.20 bump, PR #168, skipped
# this step): local tsan-native build with the block deleted, after the
# tst-rist register-before-start fix — classes 1 + 2 re-fire (3 reports:
# rist_receiver_recv_data rist-common.c:642 vs receiver_pthread_dataout
# rist-common.c:4538; ctx_del/evsocket_destroy libevsocket.c:92/352 vs
# the evsocket_create mutex init), class 3 did not fire in that run but
# is timing-dependent — block kept whole. The race that was NEW in
# 0.2.20 (sender_pthread_protocol vs rist_stats_callback_set) was a
# tst-rist call-order bug, fixed in code, deliberately NOT suppressed.
race:rist_receiver_recv_data
race:rist_receiver_associate_flow
race:receiver_pthread_dataout
race:ctx_del
race:evsocket_destroy
race:evsocket_loop_single
race:rist_sender_enqueue
race:rist_sender_flow_statistics

# 2026-09-16 — libsrt-internal race on the listener-closed exit of
# srt_accept (PR #233 tsan-native, run 35175171152). After a cross-thread
# srt_close() wakes a parked accept — the sanctioned Listener::cancel_handle()
# wake — CUDTUnited::accept reads the listener's config through its raw
# CUDTSocket* (api.cpp:1310, `ls->core().m_config.bSynRecving` on the
# "listener socket is closed" path) with no lock, while the GC thread's
# checkBrokenSockets() -> removeSocket() (api.cpp:3100) frees that socket
# under m_GlobControlLock. Both racing frames are inside libsrt 1.5.7;
# the tst-srt frames are only the srt_accept caller. Exposed for the first
# time by tst-srt::loopback accept_handle::drop_guard_wakes_parked_accept_on_unwind,
# which cancels a parked listener from another thread and then WAITS for
# the GC pass to release the port — every earlier cross-thread-cancel test
# exits before the GC free lands, so TSan never saw the pair. Upstream-report
# candidate (a libsrt UAF window, not a tst-srt bug). Scoped to the ONE
# libsrt function at the racing read, NOT race:srt* — srt_* FFI frames ride
# genuine app-race stacks and must stay unsuppressed.
# Remove/re-test: on any libsrt bump — delete the entry, dispatch
# tsan-native, re-add only if it re-fires.
race:srt::CUDTUnited::accept
